# PropCopilot completion backlog

Tracked by the 20-iteration loop. Mark items ✅ with commit hash when shipped.

## Tier A — Auth + profile

- [x] **A1** Forgot-password flow — new `/forgot-password` + `/reset-password` pages; link on sign-in; uses `supabase.auth.resetPasswordForEmail` + `updateUser`. *(pending commit)*
- [x] **A2** Logo upload — migration adds `user_profiles.logo_url`; Supabase Storage bucket `user-logos` + RLS; replace `<div>LOGO</div>` in ProfileForm with file input + crop preview; display in SignatureBlock + proposal header. *(pending commit; migration 006 needs manual application via SQL Editor)*
- [x] **A3** Brand colors applied to proposal — inject `user_profiles.brand_colors[]` as CSS vars overriding `--accent-base` etc. on `.proposal-paper`; live preview in BrandPalette. *(pending commit)*

## Tier B — AI/pipeline depth

- [x] **B1** Real proposal-refine chat (also kills C1 stub) — new `app/api/meetings/[id]/proposal/refine/route.ts` (streaming via `lib/claude.ts`); new `RefineDrawer.tsx`; replace toast stub at proposal/page.tsx:156. *(pending commit)*
- [x] **B2** Use `detected_product_ids` in generation — merge attached + detected in `lib/claude.ts`; confirm/dismiss UI in Q&A. *(pending commit)*
- [x] **B3** Smarter `deal_status` auto-transitions — share → `proposal_sent`; public open → record `opened_at`; past-due `upcoming` → surface in dashboard. *(pending commit; migration 007 needs manual application)*

## Tier D — A11y + polish (overflow)

- [x] **D1** Loading states (skeletons across dashboard/profile/meeting) *(pending commit)*
- [x] **D2** Error boundaries (`error.tsx` per route group) *(pending commit)*
- [x] **D3** Form validation (accessible inline errors) *(pending commit)*
- [x] **D4** Keyboard nav (skip-to-content, focus traps, restore on close) *(pending commit)*
- [x] **D5** ARIA labels (icon-only buttons, dropdown roles) *(pending commit; codebase already had aria-label on icon-only buttons; this commit adds sidebar landmarks + aria-current)*
- [ ] **D6** Color contrast audit (WCAG AA)
- [ ] **D7** Optimistic UI parity (proposal status, attendee mutations)
- [ ] **D8** Final smoke + `RELEASE.md`

## Iteration log

(Filled as work ships.)
